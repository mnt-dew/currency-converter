spring:
  application:
    name: currency-converter
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/currency_converter_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
